# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
.venv-test/
venv/

# PlatformIO (firmware build output is ignored in firmware/.gitignore)
.pio/

# OS
.DS_Store

# Agent worktrees: per-session scratch, not project state.
# Scoped to worktrees/ ONLY — .claude/ itself may later hold tracked agent
# definitions or settings, and a blanket ignore would silently prevent that.
.claude/worktrees/
